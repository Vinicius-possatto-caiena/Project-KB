name: Auto-fill Start Date on WIP
on:
  issues:
    types: [opened, edited]
  project_card:
    types: [moved]

jobs:
  set-start-date:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue;
            // Verificar se entrou em WIP
            // Adicionar data no campo "start date"
